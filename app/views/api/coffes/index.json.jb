@coffes.map do |coffee|
{
  id: coffee.id,
  name: coffee.name,
  price: coffee.price,
  size: coffee.size,
  description: coffee.description
}
render partial: "coffee.json.jb", locals: {coffee: coffee}
end